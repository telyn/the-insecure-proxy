This is a rust application with just one binary. Use cargo to build and test.

Never run `cd` - just call `cargo` on its own :-)
Stop and ask me to re-prompt if you fail to receive output from the terminal: I think there's an issue with the
integration with my IDE.