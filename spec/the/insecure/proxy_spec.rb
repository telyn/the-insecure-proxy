# frozen_string_literal: true

RSpec.describe The::Insecure::Proxy do
  it "has a version number" do
    expect(The::Insecure::Proxy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
